<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="DisposerTheme" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="android:actionBarTabStyle">@style/MyActionBar</item>
        <item name="android:actionBarSize">200dp</item>
        <item name="android:actionBarTabBarStyle">@style/MyActionBar</item>
    </style>

    <style name="MyActionBar" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <item name="android:height">100dp</item>
        <item name="android:background">#C1832C</item>
        <item name="android:backgroundStacked">#C1832C</item>
    </style>

</resources>
